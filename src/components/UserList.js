import React from 'react';
import UserItem from './UserItem';

function UserList() {
  return (
    <React.Fragment>
      {/* .map(m => 
        <UserItem />
      )*/}
    </React.Fragment>
  );
};

export default UserList;